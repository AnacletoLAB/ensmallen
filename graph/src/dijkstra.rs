use super::*;

impl Graph {

    /// Return the list of nodes that forms the shortest paths between root and dest
    pub fn dijkstra(&self, root: u64, dest: u64) -> Vec<u64> {
        vec![]
    }

}